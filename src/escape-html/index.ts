/* eslint-disable sukka/unicorn/prefer-export-from -- deprecate re-export */
import { escapeHTML as A, unescapeHTML as B } from 'fast-escape-html';

/** @deprecated This re-export is deprecated. Install and import from 'fast-escape-html' directly instead. This will be removed in the next major version. */
export const escapeHTML = A;
/** @deprecated This re-export is deprecated. Install and import from 'fast-escape-html' directly instead. This will be removed in the next major version. */
export const unescapeHTML = B;
