import { escapeRegexp } from 'fast-escape-regexp';
/** @deprecated This re-export is deprecated. Install and import from 'fast-escape-regexp' directly instead. This will be removed in the next major version. */
// eslint-disable-next-line sukka/unicorn/prefer-export-from -- deprecated re-export
export const escapeStringRegexp = escapeRegexp;
